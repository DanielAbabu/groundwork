# Groundwork Platform — Master Implementation Plan & Completion Checklist

Full audit and architectural refinement plan for the Groundwork engineering platform. The goal: elevate the codebase to **production-grade, resilient, and portfolio-ready** quality.

---

## Current State Summary

| Metric                      | Value                                            |
| --------------------------- | ------------------------------------------------ |
| TypeScript (`tsc --noEmit`) | **0 errors ✅**                                  |
| ESLint (`eslint .`)         | **0 errors ✅** (6 benign fast-refresh warnings) |
| Unit Tests (`vitest run`)   | **14 tests passing across 5 test files ✅**      |
| Production Build            | **100% build pass ✅**                           |
| Architecture                | TanStack Start, React 19, Tailwind, Supabase     |
| Execution Sandbox           | Pyodide Web Worker (browser python pytest)       |

---

## Master Task Checklist

### Phase 1 — Code Hygiene & Dead Code Cleanup [COMPLETED]

- [x] Run Prettier formatting across entire workspace (`npx prettier --write .`)
- [x] Configure ESLint unused variable rules (`eslint.config.js`)
- [x] Clean up package metadata (`package.json` name → `groundwork`)
- [x] Remove legacy unused components (`src/components/Navbar.tsx`)
- [x] Remove redundant `.lovable/` scaffolding directory
- [x] Remove redundant Bun lockfiles (`bun.lock`, `bunfig.toml`)
- [x] Fix state debouncing bug in `src/components/NudgePopover.tsx`

### Phase 2 — Architecture & Security Hardening [COMPLETED]

- [x] Fix Supabase `listNudges` data leak security issue in `src/lib/profile.functions.ts`
- [x] Create centralized `src/lib/auth.ts` sign-out & session utility
- [x] Create unified `src/components/AppShell.tsx` navigation wrapper
- [x] Integrate `AppShell` into `src/routes/_authenticated/route.tsx`
- [x] Remove header duplication from `dashboard.tsx`, `incidents.index.tsx`, `design.index.tsx`, `profile.tsx`

### Phase 3 — System Resilience & Error Handling [COMPLETED]

- [x] Add worker execution timeout & explicit toast error feedback in `incidents.$slug.tsx`
- [x] Add error checks to `scenario_runs` and `scenario_progress` DB calls in `progress.functions.ts` & `design.functions.ts`
- [x] Remove misleading autogenerated comments from Supabase client files (`client.ts`, `auth-middleware.ts`, `auth-attacher.ts`, `client.server.ts`)
- [x] Update fallback error page (`src/lib/error-page.ts`) to match dark Paper-Ink aesthetic

### Phase 4 — UI/UX & Responsive Ergonomics [COMPLETED]

- [x] Add sticky glassmorphic navigation header to landing page (`src/routes/index.tsx`)
- [x] Improve mobile viewport ergonomics in incident room (`incidents.$slug.tsx`) with horizontal scroll file tabs
- [x] Integrate `ProgressSummary` analytics card into `dashboard.tsx`
- [x] Add horizontal scroll wrapper and section headers for `ActivityCalendar` in `profile.tsx`

### Phase 5 — Testing Infrastructure & CI Pipeline [COMPLETED]

- [x] Add scenario schema integrity unit test (`src/content/scenarios/__tests__/scenarios.test.ts`)
- [x] Add design scenarios integrity unit test (`src/content/design/__tests__/design-scenarios.test.ts`)
- [x] Add component canvas graph grading test (`src/lib/design/__tests__/graph-grading.test.ts`)
- [x] Add stage grading engine unit test (`src/lib/design/__tests__/grading.test.ts`)
- [x] Add sandbox worker configuration test (`src/lib/sandbox/__tests__/worker.test.ts`)
- [x] Add `typecheck`, `test:watch`, and `ci` scripts to `package.json`
- [x] Create GitHub Actions workflow (`.github/workflows/ci.yml`)

### Phase 6 — Comprehensive Documentation Package [COMPLETED]

- [x] Write detailed `ARCHITECTURE.md` covering system diagrams, worker sandbox model, and Supabase RLS security
- [x] Write `DESIGN.md` documenting Paper-Ink tokens, typography, glassmorphism, and responsive guidelines
- [x] Update `README.md` with setup instructions, testing commands, and project overview
- [x] Update `AGENTS.md` with AI pair programming repository rules
- [x] Document Groundwork route structure in `src/routes/README.md`

### Phase 7 — Final Build & Quality Gate Verification [COMPLETED]

- [x] TypeScript validation (`npx tsc --noEmit`) — 0 errors
- [x] ESLint validation (`npx eslint .`) — 0 errors
- [x] Vitest test suite (`npx vitest run`) — 5 test files, 14 tests passing
- [x] Production build (`npm run build`) — Successful build output

---

## Automated Quality Verification Commands

```bash
# Run complete CI verification pipeline
npm run ci
```
